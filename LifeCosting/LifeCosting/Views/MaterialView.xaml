<UserControl x:Class="LifeCosting.Views.MaterialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:LifeCosting.ViewModel"
             xmlns:local="clr-namespace:LifeCosting.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" >
    <UserControl.Resources>
        <vm:MaterialViewModel x:Key="MaterialViewModel"/>
    </UserControl.Resources>
    <UserControl.DataContext>
        <Binding Mode="TwoWay" Source="{StaticResource MaterialViewModel}"/>
    </UserControl.DataContext>
    <Grid>
        <StackPanel>
            <ComboBox ItemsSource="{Binding Material}" SelectedItem="{Binding SelectedMaterial}"
                      DisplayMemberPath="Name" SelectedIndex="0" BorderThickness="4" BorderBrush="Red"/>
        </StackPanel>
        
    </Grid>
</UserControl>
